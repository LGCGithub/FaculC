#include <stdio.h>

int main()
{
    int aux;

    printf("Digite um numero inteiro: ");
    scanf("%d", &aux);
    printf("%f", (float)aux); // type casting
    return 0;
}

// Não ocorreu nenhum erro de compilação